const additionT = (a: number, b: number): number => a + b;

const numberR: number = 45;
const numberRr: number = 10
const resulte: number = additionT(numberR, numberRr);

console.log('The result is %d', resulte);